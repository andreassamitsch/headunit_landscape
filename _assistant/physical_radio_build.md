# Dudu7 physical radio validation

Targeted build branch for the independent FYT physical tuner integration.

The workflow applies the deterministic Dudu7 source patch, compiles only `assembleGmsDudu7Debug`, publishes the validated source changes, and stores the APK under `dist/`.
