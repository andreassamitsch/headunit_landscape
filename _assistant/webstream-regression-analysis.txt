The 13.7.2 HLS build removed the radio custom cache key for every stream. That key was the existing discriminator used by the resolver for normal MP3/AAC WebRadio playback. The follow-up must retain the exact legacy path for non-HLS streams and use the keyless active-radio fallback only while an HLS MediaItem is active.
