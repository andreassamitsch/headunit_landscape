name: Dispatch Bounded Dudu7 UI Test

on:
  push:
    branches: ["main"]
    paths:
      - ".github/workflows/dispatch-dudu7-bounded-ui-test.yml"

permissions:
  actions: write
  contents: write

jobs:
  dispatch:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v6
      - name: Dispatch UI smoke test
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh workflow run dudu7-ui-test.yml --ref main
      - name: Remove dispatcher
        run: |
          rm .github/workflows/dispatch-dudu7-bounded-ui-test.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Remove bounded Dudu7 UI dispatcher [skip ci]"
          git push
