name: Build PR (disabled on bootstrap)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Temporarily disabled on the one-time bootstrap branch"
